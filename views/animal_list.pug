extends layout

block content
  h1= title

  ul
    each animal in animal_list
      li
        a(href=animal.url) #{animal.name}
        |  (#{animal.version})

    else
      li There are no animals found.